const config = {
  apiBaseUrl: process.env.REACT_APP_API_URL || 'https://eino-devin-api.vercel.app',
  einoServiceUrl: process.env.REACT_APP_EINO_URL || 'https://eino-devin-service.vercel.app'
};

export default config;
